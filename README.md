# Simon_game
掌上游戏，这是一个简单的颜色配对游戏，里面有一个微控制器或微处理器。你按下开始按键，然后游戏会点亮其中的一个面板，然后你按下那个面板，它就会点亮两个面板，你按下两个面板，以此类推，直到你再也记不住为止。
一、游戏玩法 
1. 开机初始化：上电后，游戏会进行一次自检，所有灯闪烁一次，喇叭发出提示音，进入待机状态。
2. 生成序列：游戏开始后，MCU会随机生成一个长度为1的序列（比如：绿色灯闪+对应音效）。
3. 玩家复刻：玩家需要按下和灯/声音对应的按钮，重复这个序列。
4. 难度递增：如果按对了，序列长度+1（比如变成：绿→红），再次播放完整序列让玩家记忆；按错了，游戏结束，播放失败音效并显示最终分数。
5. 挑战极限：序列会越来越长，直到玩家出错为止，记录的最高长度就是你的得分
我们需要能发出声音，发出光，能有输入，我们可以用一个非常小的微控制器来完成这个（以Arduino为基础）。Arduino是一个技术平台，基于一个Atmel小型8位处理器，而Arduino本身并非真正的处理器，也不是真正的开发板，它实际上是你如何编程它，如何理解他，他是一个工具，一个最初用java编译的工具。它在底层隐藏了编译器，包括C编译器和GNU编译器。因此，我们将使用一个兼容Arduino的处理器和一个兼容Arduino的核心设计，这样我们就能用Arduino工具来编程它。如果我们拿一个Arduino，然后只连接四个开关作为按钮，再连接四个LED灯，然后连接一些声音组件，再连接几个开关用于控制这些其他按钮，你会像编程Arduino一样来编程它。这样，通过输入/输出端口，你就可以控制所有这些按钮和灯光了。
软硬件:https://www.arduino.cc/
<img width="1918" height="960" alt="image" src="https://github.com/user-attachments/assets/0670fd0c-f480-46c2-b378-e7a16ccf3d39" />
https://store.arduino.cc/products/arduino-nano
<img width="1918" height="915" alt="image" src="https://github.com/user-attachments/assets/5af9fff1-7ec7-49a3-8a82-df50948308d0" />
NANO上面更加简洁，关键在于有些固件可以加载到这个处理器上，它仍然是ATmega328P处理器。它上面就只有几个LED,背面有一个USB端口，它实际上内置了USB转串口芯片，设计更简洁，我们将沿用nano的基本核心设计，这样当我们把自己的开发板连接到电脑，并使用Arduino工具时，电脑就会把它识别成Arduino nano，这正是我们想要的效果。然后我们会把所有的LED、灯和按钮都连接到这些GPIO接口上，所以我们打算设计一个集成大量LED、声音功能的设备。有电源系统，用于连接各种东西的引脚也就是接口排针、按钮排、led、Arduino、Arduino的编程接口（在我们给这个东西编程，把它变成一个Arduino之前，我们必须以某种方式把固件程序刷进去，这样他才能成为一个Arduino，所以我们必须通过直连编程端口来操作，还需要一个编程器，完成那一步后，我们就可以使用USB端口了）、USB转串口桥接器、最后从Arduino芯片上取几个引脚，几个来自ATmega328P芯片的GPIO口，然后我们可以直接把它们接到扬声器上或者通过一个单晶体管输出（一个简单的放大器）。但我们想做的是生成一个真正的音频信号，然后再进行滤波，再通过一个真正的音频放大器，以输出几瓦的功率。如果你只想输出一个简单的方波也能做到。也可以使用PWM脉冲宽度调制、PDM脉冲密度调制，这样你就可以播放合成语音、音乐，或者任何你想播放的，你可以连接一张SD卡，然后使用Arduino SD库，将SD卡接入音频库并播放音频片段，一旦拥有了Arduino核心，就能实现很多功能。我们将使用一个9V电池，然后有一个保护二极管，电源开关,LDO稳压器，这样我们整个系统就能获得5V供电了，我们有旁路还有旁路输出。我们有5V电源指示灯，显示电源状态良好，我们有几个测试点方便查看电源。有Arduino nano风格的排针。难度开关就是一个拨动开关，它可以左右拨动，本质上是一个单刀双掷开关，一个档位是开启状态，另一个挡位是关闭状态，所以一个档位会使它上拉，另一个档位会使它下拉到地，这样你就会得到一个1或0信号他会切换这两种模式。最初的想法是我们有这么多的按钮，红黄蓝绿这些彩色按钮，然后是开始键、最后一个键、最长键，还有一个额外的辅助按钮，不知道你会用它来做什么，如果我们只是把所有这些按钮都接到GPIO上，我们就得用8个GPIO口。但是因为我们一次只能按下一个按钮，所以我们使用一个解复用器8对3的，每当你按下其中一个按钮时，你会得到一个3比特的代码。还有另一种方法也能做到这一点，我们可以将数据串行化，这样就能使用一个8比特的移位寄存器并行锁存这8比特数据，然后我们就可以通过串行方式将其传输到处理器，但我们仍需要三个引脚，原因如下：我们需要一个锁存引脚、一个时钟引脚或者一个移位引脚、一个数据引脚,但这种方式在超过8个之后会变得更高效，所以，如果我们想要16个或更多开关，我们就会采用串行化方式。显示模块：我们只是直接从GAL芯片获取输出，然后通过LED显示。如果查看mega的数据手册并查看I/O的电流承载能力，可能是20A。如果这些LED不能正常工作，得加个场效应管或晶体管，但只用GPIO口应该没有问题，如果不行就会加上场效应管或晶体管，这样我们就能在需要时，往里面灌入大量电流，如果你想要大功率的灯也没问题。这些200欧姆的电阻只是占位符，可以把他们设为0欧姆或10欧姆，它们只是用来限流的。手动复位。Arduino nano设计。放大器，音频信号经过低通滤波器然后有一个耦合电容一个电阻Rin，Rf和Rin的比率决定了增益。我们其实不太在意电压增益，因为我们往这里输入的是5V信号，所以我们会有足够大的电压，我们会更关心电流增益，而这是这个东西能提供给我们的，他是1.1瓦，所以这是一个很不错的放大器，然后他就直接连接到扬声器了。这是一个桥接式负载，其中一侧扬声器并没有接地，所以这意味着他能翻转整个信号，这样你就能获得非常宽的振幅范围，他不是以地为参考的，所以这是一个很棒的小型放大器。USB，串行UART，使用的是FT231XS-R，其中一些内容来自FTDI的参考设计，27欧姆的电阻是为了防止反射，因为它们可能会变成传输线当你在处理USB的D+和D-信号时。有趣的是USB连接器本身，连接器本身上有接地线，有数据线，我们这里不使用ID引脚，还有VBUS，也就是USB端口提供的5V电压。现在有个问题，USB端口根本不能保证输出5V电压，它们的电压范围在4.75V到5.5V之间。总之任何连接到USB电源的设备，最好都加一个小保险丝，我们这里有一个小铁氧体磁珠来抑制噪声，我们不希望噪声被注入到我们的系统中，我们也不想将噪声反馈给USB。这里有几个电容，用于旁路滤波，以获得一个干净的。VCC_USB只是一个标记，最后还有一个保护二极管，这样电流就不能从这个方向流进来。然后我们有一个VCC_5V，它将连接到我们电源另一个VCC_5V。
总之，我们有一个Arduino，Arduino的I/O接口（排针),还有电源，而Arduino nano在这方面有所不同，他甚至没有独立电源，他只是通过USB供电。我们有自己的电源，因为他不是电池供电的。接着我们这里有一个开关多路复用器，这样我们就可以不增加成本地拥有很多开关了。接着我们还有小LED以及编程端口，我们还有一个音频放大器。


我们还希望所有的无源元件都采用表面贴装器件,我们选用0805来制作这些元器件。
<img width="1197" height="607" alt="image" src="https://github.com/user-attachments/assets/cdfc85bf-83a1-469f-a68f-0e1edd2165d1" />
他这里用的是16MHz晶振，有两个电容没有在设计图中标出来。他还有模拟参考线AREF（20号引脚），它通过一个电容接到地线了，这是模拟参考电压，所以这应该连接到你想要的参考电压上。他把所有的模拟VCC，共三个都连接起来了，这样模拟信号线就会有噪声，我们希望他更干净一些。而且上面的参考资料显示他们属于不同的电源域，他们通过一个铁氧体磁珠和各自的旁路电容隔离开来。有一个1000欧姆的上拉电阻接到复位端，旁边还有一个小开关。13号引脚是SPI接口的SCLK信号，MOSI是主出串入。SPI接口即串行外设接口，它有四路信号：时钟信号、数据输入、数据输出和片选信号。接下来要在上面加载固件，我们把固件加载进去，这样他就有了Arduino的个性。而要实现数据的进出，它会用到固件加载完成后，接着与引导程序进行通信。所以微控制器在以这种Arduino特性启动后，它会带有一个引导程序，引导程序对软件的作用是：当处理器启动时，他会等待一个程序，这个程序可以通过SPI、I2C、无线等方式载入，在这里它是通过简单的数字串行接口载入的，通过TX和RX引脚。在TX和RX引脚这里用到1K电阻的原因：这组TX和RX会连接到这个USB,但它也会把这些引脚引出来，这些引脚被引出作为Arduino接口，不过要小心，因为他是在电阻之前就把这些引脚引出，然后他让这些电阻连接到USB。这样做的好处是可以应对突发状况，也就是说，如果你拉扯D1/TX和D0/RX这些线，强行将其置为0或1，如果我们直接将USB短路，可能会造成一些损坏，所以电阻能将电流降到最低。所以如果你有一个1K的电阻和一个5V信号，显然电流会非常小，最终通过这里的电流最大也只有几安培，任何东西都不会被损坏。所以这些引脚被引出在设计中也很重要。
<img width="937" height="730" alt="image" src="https://github.com/user-attachments/assets/94772746-9569-4242-a0c7-2128ef34b982" />
USB转接桥：这是一个USB转串口的桥接器，叫FTDI
<img width="1138" height="565" alt="image" src="https://github.com/user-attachments/assets/ff87b1da-e01c-416f-88ea-5452f43548ab" />
<img width="1918" height="942" alt="image" src="https://github.com/user-attachments/assets/7ec8a874-fa58-4d62-be40-57853d465f7d" />




